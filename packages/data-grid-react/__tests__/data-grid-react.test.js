'use strict';

const dataGridReact = require('..');
const assert = require('assert').strict;

assert.strictEqual(dataGridReact(), 'Hello from dataGridReact');
console.info('dataGridReact tests passed');
